<%= form_for(@shared_project) do |f| %>
  <% if @shared_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shared_project.errors.count, "error") %> prohibited this shared_project from being saved:</h2>

      <ul>
      <% @shared_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
