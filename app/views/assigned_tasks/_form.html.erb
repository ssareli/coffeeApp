<%= form_for(@assigned_task) do |f| %>
  <% if @assigned_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assigned_task.errors.count, "error") %> prohibited this assigned_task from being saved:</h2>

      <ul>
      <% @assigned_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.text_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.text_field :owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
