<%= form_for(@smart_list) do |f| %>
  <% if @smart_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smart_list.errors.count, "error") %> prohibited this smart_list from being saved:</h2>

      <ul>
      <% @smart_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
